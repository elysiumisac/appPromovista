# Promovista Frontend

React Native (Expo) client. Login via phone OTP using Supabase and view available campaigns.

Create a `.env` file based on `.env.example` then run:
```bash
npm install
expo start
```
